@model List<MyAppFirst.Models.Item>

@{
    ViewData["Title"] = "Indice";
}

<h3>Indice</h3>
<table class="table">
    <thead>
        <tr>
            <th>Id</th>
            <th>Description</th>
            <th>Price</th>
            <th>Operation</th>
        </tr>
    </thead>
    <tbody>
        @foreach(var item in Model)
        {
            <tr>
                <td>@item.Id</td>
                <td>@item.Description</td>
                <td>@item.Price.ToString("N2")</td>
                <td>
                    <a asp-action="Edit">Edit</a>
                    <a asp-action="Delete">Delete</a>
                </td>
            </tr>    
        }
    </tbody>
</table>
